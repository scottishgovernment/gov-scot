definitions:
  config:
    /hippo:configuration/hippo:translations/hippo:cms/validators/en:
      non-empty: A mandatory field is empty
      publication-slug-spaces: Slugs must not contain spaces.
      publication-slug-dots: Slugs must not contain full stops.
      govscot-slug: This slug is either already in use, the field has been left blank
        or it contains an invalid character. Please choose another.
      govscot-placeholder: replace the placeholder string inside []
