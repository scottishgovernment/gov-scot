define([], function () {
    'use strict';

    var defaultPage = {};

    defaultPage.init = function () {
    };

    window.format = defaultPage;
    window.format.init();

    return defaultPage;
});
