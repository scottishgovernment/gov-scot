package scot.gov.www.beans;

import org.junit.Test;

public class EmptyTest {

    @Test
    public void emptyTest() {
        // This project contains only bean classes.
        // This 'test' ensures that the build generates at least one test result file, preventing the build from
        // failing because of lack of tests.
    }

}
